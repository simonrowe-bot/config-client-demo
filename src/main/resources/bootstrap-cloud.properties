spring.cloud.config.enabled=true
spring.cloud.config.uri=${vcap.services.jdbc-config-server.credentials.url}
spring.cloud.config.password=${vcap.services.jdbc-config-server.credentials.password}
spring.cloud.config.username=${vcap.services.jdbc-config-server.credentials.username}